{
  "name": "app",
  "private": "true",
  "dependencies": {
    "bootstrap": "^5.3.0",
    "@popperjs/core": "^2.11.8",
    "sass": "^1.77.0",
    "bootstrap-icons": "^1.11.0"
  },
  "scripts": {
    "build:css": "sass app/assets/stylesheets/application.bootstrap.scss:app/assets/builds/application.css --no-source-map --load-path=node_modules",
    "work:start": "node scripts/work-start.js",
    "dev:branch": "node scripts/create-branch.js",
    "dev:commit": "node scripts/auto-commit.js",
    "dev:push": "node scripts/auto-push.js",
    "dev:pr": "gh pr create --draft --title 'WIP: Auto-generated PR' --body 'Auto-generated pull request'",
    "pr:create": "echo '🚀 Claude CodeでPR作成中...' && claude --print 'このブランチの変更内容を分析して、高品質なPR本文と共にプルリクエストを作成してください。gh pr createコマンドを使用してEventPay Managerプロジェクトに適したPRを作成してください。'",
    "pr:update": "echo '🤖 Claude CodeでPR本文を生成中...' && claude --print 'このPRの本文を生成してgh pr editで更新してください。変更内容を分析し、EventPay Managerプロジェクトに適した高品質なPR本文を作成してください。'",
    "dev:setup": "npm run dev:branch && npm run dev:commit",
    "archive:create": "node scripts/create-archive.js",
    "git:update": "git checkout main && git pull origin main",
    "git:clean": "git branch --merged main | grep -v main | xargs -n 1 git branch -d",
    "hooks:install": "cp -r .git/hooks/* .git/hooks/ && chmod +x .git/hooks/*",
    "quality:check": "docker-compose exec web bundle exec rubocop && docker-compose exec web bundle exec rspec",
    "security:scan": "docker-compose exec web bundle exec brakeman --no-pager"
  }
}
